# Changelog

All notable changes to this project are documented here.
This file is generated by [git-cliff](https://git-cliff.org) from Conventional Commits.

Each section is a **minor line** (tag `M.m.0` opens the `M.m.x` line; commits after it are
that line's versions `M.m.<commits-since-tag>`). Within a line, commits are a **timeline**:
newest on top, oldest at the bottom, each prefixed with its conventional type. `— current`
marks the line still in progress.

## [1.1.x] — current

- *(sample)* Guard null GetEntryAssembly in demo NativeMethods ctor
- *(docs)* Note OnRender must not throw (library swallows + skips frame)
- *(sample)* Dispose D3D11Image on window close
- *(docs)* Document D3D11Image dispose lifecycle and public API
- *(fix)* Guard null GetEntryAssembly in NativeWrapper DEBUG ctor
- *(fix)* Harden managed dispose & finalizer threading
- *(fix)* Release SRW lock on OpenConsumer null-guard path
- *(fix)* Avoid double-free race in CSurface*::Release refcount
- *(fix)* D3D10 LockSurface honors DO_NOT_WAIT flag
- *(chore)* Add CodeGraph integration (mcp + claude config)
- *(fix)* Add trailing newline to version.rc (avoid rc.exe RC1004)

## [1.0.x]

- *(build)* GitVersion + native C++ versioning + robust Build.ps1

<!-- generated by git-cliff -->
